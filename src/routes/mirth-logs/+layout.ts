import type { LayoutLoad } from './$types';

export const load: LayoutLoad = async () => {
	// Load initial day data for both logs and messages
	// This will be handled by the store when the components mount
	return {};
};
